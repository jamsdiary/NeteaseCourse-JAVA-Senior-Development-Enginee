顺序消息示例

OrderedConsumer 有两种消费模式：普通消息模式和顺序消息模式，默认是普通消息模式（通过修改注1和注2部分的注释可切换）
，用于演示不使用顺序模式时，出现的消费消息顺序不一致的问题
验证方式：
1、先启动OrderedProducer发送消息，一定要先启动生产者，用于模拟消息发送出去后，消息堆积到broker
2、OrderedConsumer已普通模式启动
3、查看OrderedConsumer控制台的日志，此时会出现消息消费顺序不对的问题

注意：如果先启动的OrderedConsumer，则很难重现，所以必须先启动OrderedProducer

OrderedProducer 有两种消息发送顺序：分区顺序和全局顺序，默认是分区顺序
使用全局顺序时，所有消息都只会发送到同一个队列，所以消息消费时将按照发送时的时间顺序进行消费
如果开启多个消费者程序，可以看到只会有一个消费者进行消费

而使用分区顺序时，消息将发送到不同的队列中
开启多个消费者程序时，将可以看到每个消费者都会消费消息
所以，全局顺序的消息消费性能要低于分区顺序